module.exports = class extends think.Model {
  /**
   * 获取分类详情
   * @returns {Promise.<*>}
   */
  // async getIndex(classifyCode) {
  //   const classifyDetail = await this.where({ classify_code: classifyCode }).find();
  //   return classifyDetail;
  // }
};
